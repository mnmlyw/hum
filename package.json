{
  "name": "hum",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/parser.test.js",
    "test:e2e": "playwright test",
    "test:all": "npm run lint && npm run check:spec && npm run preset:check && npm test && npm run test:e2e",
    "check:spec": "node tools/check-spec.js",
    "preset:embed": "node tools/embed-presets.js",
    "preset:check": "node tools/embed-presets.js --check",
    "lint": "eslint tests tools",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "eslint": "^10.2.1",
    "fast-check": "^4.7.0",
    "globals": "^17.5.0",
    "husky": "^9.1.7"
  }
}
