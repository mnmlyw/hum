*.hum
# Demo fixtures (in demos/) and the recording-specific compact demo
# (in recording/) are tracked. Other .hum files (user-saved songs in
# the project root, etc.) stay ignored.
!demos/*.hum
!recording/*.hum
node_modules/
test-results/
playwright-report/
.claude/
